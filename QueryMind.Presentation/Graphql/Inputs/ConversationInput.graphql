input CreateConversationInput {
  name: String
  userId: ID!
}

input DeleteConversationInput {
  conversationId: ID!
  userId: ID!
}

input SendMessageInput {
  conversationId: ID!
  content: String!
}
